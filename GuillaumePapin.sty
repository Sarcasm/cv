%% Following more or less the same as raphink (https://github.com/raphink/CV).

\ProvidesPackage{GuillaumePapin}

%% moderncv themes
\moderncvtheme[grey]{classic} % optional argument are 'blue' (default), 'orange', 'red', 'green', 'grey' and 'roman' (for roman fonts, instead of sans serif fonts)

%% Character encoding
\usepackage[utf8]{inputenc}    % replace by the encoding you are using
\usepackage[super]{nth}

%% Microtype - small typographic enhancement
\usepackage[protrusion=true,expansion=true]{microtype}

%% Personal data
\familyname{Papin}
\firstname{Guillaume}
\email{guillaume.papin@epitech.eu} % optional, remove the line if not wanted
%% \homepage{sarcasm.github.com} % optional, remove the line if not wanted

%% Only for the french version
%% \photo[64pt]{picture} % '64pt' is the height the picture must be resized to and 'picture' is the name of the picture file; optional, remove the line if not wanted

%% Adjust the page margins
\usepackage[scale=0.88]{geometry}
\setlength{\hintscolumnwidth}{2.6cm} % if you want to change the width of the column with the dates
\AtBeginDocument{\recomputelengths} % required when changes are made to page layout lengths

%% Suppress automatic page numbering for CVs longer than one page
\nopagenumbers{}

%% List style
%% \renewcommand{\listitemsymbol}{-} % change the symbol for lists

%% Colored links
%% see http://tex.stackexchange.com/a/14309
\newcommand\Colorhref[3][cyan]{\href{#2}{\small\color{#1}#3}}
%% Usage (color default to cyan):
%% \Colorhref[COLOR]{http://LINK.com}{LINK NAME}

% Objectives
%% \newcommand*{\cvobjective}[2]{%
%%   \quote{\itshape #1 \\ \scshape \footnotesize #2}}

% Change from the default quote
\newcommand*{\myquote}[2]{%
  \quote{\itshape #1 \\ \scshape \footnotesize #2}}

% Hobbies & Interests
%% \newcommand*{\cvhobby}[2]{\cvline{\textbf{#1}}{\small #2}}
\newcommand*{\cvhobby}[2]{\cvline{#1}{\small #2}}

%% Timeline
\usepackage[firstyear=2006, lastyear=2013]{moderntimeline}
\tlwidth{0.8ex}

%% My colors
%% \definecolor{myColor}{rgb}{1,0.5,0}
\definecolor{dark-grey}{gray}{0.30}
